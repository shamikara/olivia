# Admin application

Reserved for the independently deployed Commerce Admin application. The current admin UI is available at `apps/storefront/app/admin` while this app is separated into a dedicated workspace.
