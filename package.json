{
  "name": "olivia-glow-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*", "packages/commerce/*"],
  "scripts": {
    "dev": "npm run dev --workspace @olivia/storefront",
    "build": "npm run build --workspace @olivia/storefront",
    "start": "npm run start --workspace @olivia/storefront",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
